import React from 'react'

function randlnt(max, min) {
    let x = (min,max) + 1
  return (
    min + Math.floor(Math.random() * x)
  )
}

export default function Banner() {
    let banner = ""
    let url = ""

    switch(r){
        case 1 : 
            banner = '../public/banners/'
    }
}